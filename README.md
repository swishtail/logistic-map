# logistic-map
Plotting and rendering of fixed points of the logistic map in Julia, with the `CairoMakie` library. The Scheme came first, just to test ideas.

3.8 < r < 3.88 and 0.4 < x < 0.6:

![Fixed points of the logistic map](logistic_fixed_points.png)
